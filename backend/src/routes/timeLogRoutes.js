'use strict';
const router = require('express').Router();

// Placeholder TimeLog routes (to be implemented)
router.get('/', (req, res) => {
  res.json({ success: true, message: 'TimeLog routes OK' });
});

module.exports = router;

